test3 takes converged structures from test2 and try to run them again with:
- DirectCellOpt
- lower EPS = 1E-7 (instead of 1E-6)
- lower convergence for forces RMS/MAX = default CP2K (instead of ~2.5x)
